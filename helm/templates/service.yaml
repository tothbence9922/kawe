apiVersion: v1
kind: Service
metadata:
  name: {{ include "kawe.fullname" . }}
  labels:
    {{- include "kawe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kawe.selectorLabels" . | nindent 4 }}
